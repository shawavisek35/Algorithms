#include <iostream>
using namespace std;

void solve(int n)
{
    int wt = new int(n),i,j;
    int c = new int(n);

    for(i=0;i<n;i++)
    {
        cin >> wt[i] >> c[i];
    }

    
}

int main()
{
    int n;
    cin >> n;
    solve(n);
    return 0;
}