#include<bits/stdc++.h>
using namespace std;

int main(){
    int n,a,b, count=0;
    cin >> n;
    unordered_map<int,int> u;
    for(int i=0;i<n;i++){
        cin >> a >> b;
        u[a] = b;
    }

    for(auto t : u){
        if(u[t.first] == )
    }
        
    cout << count << "\n";
    return 0;
}